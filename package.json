{
  "private": true,
  "scripts": {
    "temp-build": "yarn workspaces run build",
    "build": "node buildSharedLibraries.js && yarn workspaces run build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
